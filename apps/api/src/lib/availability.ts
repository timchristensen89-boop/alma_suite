// Availability matching lives in @alma/shared so the roster board in the
// browser and the API apply exactly the same rules. Re-exported here so
// existing imports keep working.
export * from '@alma/shared';
